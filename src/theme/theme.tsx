"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1E549F",
      light: "#5FC9F3",
      dark: "#081F37",
      contrastText: "#2E79BA",
    },
  },
});

export default theme;
